# Plaintext

The plaintext is a simple wrapper around BSON's String type.

## Plaintext Format

```json5
{
  "plaintext": "Hello World!",
  // standard fields:
  "provider": "plaintext",
  "version": "0.0.0",
}
```